twig:
    file_name_pattern: '*.twig'
    form_themes:
        - 'bootstrap_5_layout.html.twig'
        - 'bootstrap_switch_layout.html.twig'
    globals:
        DATETIME_FORMAT: 'Y-m-d H:i'
        DATE_FORMAT: 'Y-m-d'
        currentSport: '@App\Service\CurrentSportProvider'

when@test:
    twig:
        strict_variables: true
